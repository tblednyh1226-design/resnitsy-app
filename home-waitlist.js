/* Home waitlist: one request = one card, with unread markers only for unseen actions. */
(function(){
  const ACTIVE=['active','offered'];
  const statusName={active:'Ждёт окошко',offered:'Предложено',accepted:'Согласился',expired:'Истёк срок',cancelled:'Отменено'};
  const eventName={created:'Запрос создан',updated:'Параметры изменены',offered:'Предложено окно',declined:'Клиент отказался',accepted:'Клиент согласился',expired:'Истёк срок ожидания',session_passed:'Запись уже прошла',cancelled:'Ожидание отменено',reactivated:'Снова активен'};
  let unread={};
  function fmtDate(x){if(!x)return '—';return new Date(x+'T00:00:00+03:00').toLocaleDateString('ru-RU',{timeZone:'Europe/Moscow',day:'numeric',month:'short'})}
  function fmtDT(x){if(!x)return '';return new Date(x).toLocaleString('ru-RU',{timeZone:'Europe/Moscow',day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'})}
  function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
  function details(r){const time=(r.time_from||r.time_to)?`${String(r.time_from||'').slice(0,5)}–${String(r.time_to||'').slice(0,5)}`:'Любое время';return {dates:`${fmtDate(r.date_from)} — ${fmtDate(r.date_to)}`,time}}
  async function loadUnread(){try{unread=await rpc('master_waitlist_unread',{p_pin:PIN})||{}}catch{unread={}}return unread}
  async function refreshCounter(){try{const z=await waitApi('list',{}),n=(z.rows||[]).filter(r=>ACTIVE.includes(r.status)).length;await loadUnread();const newN=Object.values(unread).reduce((a,b)=>a+Number(b||0),0);const badge=document.getElementById('homeWaitlistCount');if(badge){badge.textContent=newN||n;badge.classList.toggle('is-new',!!newN);badge.hidden=!(newN||n)}const sub=document.getElementById('homeWaitlistSub');if(sub)sub.textContent=newN?`Новых действий: ${newN}`:(n?`Активных запросов: ${n}`:'Сейчас никто не ждёт окошко');return n}catch{return 0}}
  async function loadEvents(rows){await Promise.all(rows.map(async r=>{try{const z=await waitApi('history',{id:r.id});r._events=z.rows||[]}catch{r._events=[]}}));return rows}
  function eventHtml(x,rid){const isNew=!x.master_seen_at&&Number(unread[rid]||0)>0;return `<div class="wl-event"><span class="wl-event-dot ${isNew?'unread':''}"></span><div><b>${esc(eventName[x.event_type]||x.event_type||'Событие')}</b><div class="sub">${fmtDT(x.created_at)}</div>${x.event_text?`<div class="wl-event-text">${esc(x.event_text)}</div>`:''}${x.offered_at?`<div class="sub">Окно: ${fmtDT(x.offered_at)}</div>`:''}</div></div>`}
  function timeline(r){const ev=r._events||[];if(!ev.length)return '<div class="sub">История этого запроса пока пустая</div>';return `<div class="wl-timeline">${ev.map(x=>eventHtml(x,r.id)).join('')}</div>`}
  function card(r,isActive){const c=r.clients||{},d=details(r),initial=(c.display_name||'К').trim().charAt(0).toUpperCase(),newCount=Number(unread[r.id]||0);return `<div class="wl-request-card" data-request="${r.id}"><div class="wl-card-top wl-seen" data-id="${r.id}"><div class="wl-avatar">${esc(initial)}${newCount?'<span class="wl-new-dot"></span>':''}</div><div class="wl-person"><b>${esc(c.display_name||'Клиент')}</b><div class="sub">${esc(c.phone||'')}</div></div><span class="wl-status ${r.status}">${statusName[r.status]||r.status}</span></div><div class="wl-info-grid"><div class="wl-info"><span>Ждёт</span><b>${d.dates}</b></div><div class="wl-info"><span>Время</span><b>${d.time}</b></div><div class="wl-info wl-service"><span>Процедура</span><b>${esc(r.desired_text||'Не указана')}</b></div></div><div class="wl-history-head wl-seen" data-id="${r.id}"><b>История этого запроса</b>${newCount?`<span class="wl-new-label">новых: ${newCount}</span>`:''}</div>${timeline(r)}${isActive?`<div class="wl-actions">${r.status==='offered'?`<button class="btn primary wl-accept" data-id="${r.id}">✓ Согласился</button><button class="btn wl-decline" data-id="${r.id}">× Отказался</button>`:`<button class="btn primary wl-offer" data-id="${r.id}">Предложить окно</button>`}<button class="btn wl-client" data-client="${r.client_id||''}">Клиент</button></div>`:`<div class="wl-actions"><button class="btn wl-client" data-client="${r.client_id||''}">Клиент</button></div>`}</div>`}
  function bindRows(rows,rerender){document.querySelectorAll('.wl-seen').forEach(b=>b.onclick=async()=>{const id=b.dataset.id;if(Number(unread[id]||0)>0){await rpc('master_mark_waitlist_seen',{p_pin:PIN,p_request_id:id});unread[id]=0;await refreshCounter();await rerender()}});document.querySelectorAll('.wl-client').forEach(b=>b.onclick=()=>{const id=b.dataset.client;if(id)clientCard(id)});document.querySelectorAll('.wl-offer').forEach(b=>b.onclick=()=>offerForm(rows.find(x=>x.id===b.dataset.id),rerender));document.querySelectorAll('.wl-accept').forEach(b=>b.onclick=async()=>{if(!confirm('Клиент согласился на предложенное окно? Запрос уйдёт в историю.'))return;await waitApi('accept',{id:b.dataset.id});await refreshCounter();await rerender()});document.querySelectorAll('.wl-decline').forEach(b=>b.onclick=async()=>{const note=prompt('Комментарий к отказу:','Клиент отказался от этого окна')||'Клиент отказался от этого окна';await waitApi('decline',{id:b.dataset.id,text:note});await refreshCounter();await rerender()})}
  function offerForm(r,rerender){const d=todayStr();modal('Предложить окно',r?.clients?.display_name||'',`<div class="field"><label>Дата</label><input id="wlOfferDate" type="date" value="${d}"></div><div class="field"><label>Время</label><input id="wlOfferTime" type="time" value="10:00"></div><div class="field"><label>Комментарий</label><textarea id="wlOfferNote" rows="4"></textarea></div>`,`<button class="btn primary" id="wlOfferSave">Отметить как предложенное</button><button class="btn" id="wlOfferCancel">Отмена</button>`);document.getElementById('wlOfferCancel').onclick=closeModal;document.getElementById('wlOfferSave').onclick=async()=>{const ds=document.getElementById('wlOfferDate').value,t=document.getElementById('wlOfferTime').value;if(!ds||!t)return alert('Укажите дату и время');await waitApi('offer',{id:r.id,offeredAt:new Date(ds+'T'+t+':00+03:00').toISOString(),text:document.getElementById('wlOfferNote').value.trim()||`Предложено окно ${fmtDate(ds)} в ${t}`});closeModal();await rerender()}}
  async function openWaitlistOverview(initial='active'){try{const z=await waitApi('list',{}),all=z.rows||[],active=all.filter(r=>ACTIVE.includes(r.status)),archive=all.filter(r=>!ACTIVE.includes(r.status));await Promise.all([loadEvents(all),loadUnread()]);const body=`<div class="wl-tabs"><button class="btn wl-tab ${initial==='active'?'on':''}" data-tab="active">Активные <b>${active.length}</b></button><button class="btn wl-tab ${initial==='history'?'on':''}" data-tab="history">История <b>${archive.length}</b></button></div><div id="wlHomeList"></div>`;modal('Ловец окошек','Каждый запуск ловца — отдельная карточка',body,'<button class="btn" id="wlHomeClose">Закрыть</button>');let tab=initial;const render=async()=>{const host=document.getElementById('wlHomeList');if(!host)return;const rows=tab==='active'?active:archive;host.innerHTML=rows.length?`<div class="wl-request-list">${rows.map(r=>card(r,tab==='active')).join('')}</div>`:`<div class="card"><b>${tab==='active'?'Активных запросов нет':'История пока пустая'}</b></div>`;bindRows(rows,async()=>{closeModal();await openWaitlistOverview(tab)})};document.querySelectorAll('.wl-tab').forEach(b=>b.onclick=async()=>{tab=b.dataset.tab;document.querySelectorAll('.wl-tab').forEach(x=>x.classList.toggle('on',x===b));await render()});document.getElementById('wlHomeClose').onclick=closeModal;await render()}catch(e){alert(e.message)}}
  const grid=document.querySelector('#home .homegrid');if(grid&&!document.getElementById('homeWaitlistBtn')){const b=document.createElement('button');b.className='homebtn wl-homebtn';b.id='homeWaitlistBtn';b.innerHTML='<span id="homeWaitlistCount" class="wl-count" hidden>0</span><strong>Ловец окошек</strong><span id="homeWaitlistSub">Клиенты, которые ждут свободное время</span>';b.onclick=()=>openWaitlistOverview('active');grid.appendChild(b)}
  const st=document.createElement('style');st.textContent=`.wl-homebtn{position:relative}.wl-count{position:absolute;right:12px;top:10px;min-width:27px;height:27px;padding:0 7px;border-radius:999px;background:#c8bcc4;color:#fff;display:grid;place-items:center;font-size:13px;font-weight:800}.wl-count.is-new,.wl-new-dot,.wl-event-dot.unread{background:#ff3b7a;box-shadow:0 0 0 4px #ffddea}.wl-new-dot{position:absolute;width:12px;height:12px;border-radius:50%;right:-1px;top:-1px}.wl-avatar{position:relative}.wl-new-label{font-size:10px;color:#ff3b7a;margin-left:auto}.wl-history-head{display:flex;align-items:center;gap:8px}.wl-event-dot{width:10px;height:10px;border-radius:50%;background:#d9d2d7;margin:5px}.wl-event-dot.unread{width:12px;height:12px;margin:4px}.wl-request-list{display:grid;gap:12px}.wl-request-card{border:1px solid var(--line);background:#fff;border-radius:16px;padding:13px}.wl-card-top{display:grid;grid-template-columns:44px 1fr auto;gap:10px;align-items:center}.wl-avatar{width:44px;height:44px;border-radius:50%;background:var(--soft);display:grid;place-items:center;font-weight:800}.wl-status{font-size:10px;padding:5px 8px;border-radius:999px;background:#eee}.wl-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:12px}.wl-info{border:1px solid #eee;border-radius:11px;padding:9px}.wl-info span{display:block;font-size:10px;color:var(--muted)}.wl-service{grid-column:1/-1}.wl-history-head{border-top:1px solid #eee;margin-top:12px;padding-top:11px;margin-bottom:8px}.wl-event{display:grid;grid-template-columns:20px 1fr;gap:8px;padding:0 0 11px}.wl-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.wl-actions .btn{flex:1}`;document.head.appendChild(st);window.openWaitlistOverview=openWaitlistOverview;window.refreshWaitlistCounter=refreshCounter;refreshCounter();
})();