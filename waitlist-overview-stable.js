/* Slotelly waitlist overview v3: instant snapshot + fast backend refresh. */
(function(){
 const FAST='https://acukaqoguzkrphauovhk.supabase.co/functions/v1/resnitsy-fast',esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),statusLabel=s=>({active:'Ждёт',new:'Новый',offered:'Предложено',accepted:'Согласился',expired:'Истёк',cancelled:'Отменено'})[s]||s||'';
 const dateTxt=x=>!x?'—':new Date(x+'T12:00:00+03:00').toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit',timeZone:'Europe/Moscow'});
 function renderRows(host,all,initial='active'){const active=all.filter(r=>['active','new','offered'].includes(r.status)),archive=all.filter(r=>!['active','new','offered'].includes(r.status));let tab=initial;const render=()=>{const rows=tab==='active'?active:archive;host.innerHTML=`<div style="display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-bottom:10px"><button class="btn ${tab==='active'?'primary':''}" data-wltab="active">Активные ${active.length}</button><button class="btn ${tab==='history'?'primary':''}" data-wltab="history">История ${archive.length}</button></div><div class="list">${rows.length?rows.map(r=>{const c=r.clients||{};return `<div class="card"><div style="display:flex;justify-content:space-between;gap:8px"><b>${esc(c.display_name||'Клиент')}</b><span class="sub">${esc(statusLabel(r.status))}</span></div><div class="sub">${esc(c.phone||'')}</div><div style="margin-top:7px">${esc(r.desired_text||'Ожидает свободное окно')}</div>${r.date_from||r.date_to?`<div class="sub">${dateTxt(r.date_from)}–${dateTxt(r.date_to)}</div>`:''}${r.client_id?`<button class="btn wlStableClient" data-client="${esc(r.client_id)}" style="width:100%;margin-top:8px">Открыть клиента</button>`:''}</div>`}).join(''):'<div class="sub" style="padding:14px">Нет запросов</div>'}</div>`;host.querySelectorAll('[data-wltab]').forEach(b=>b.onclick=()=>{tab=b.dataset.wltab;render()});host.querySelectorAll('.wlStableClient').forEach(b=>b.onclick=()=>clientCard(b.dataset.client))};render()}
 async function api(){const ctl=new AbortController(),timer=setTimeout(()=>ctl.abort(),3500);try{const r=await fetch(FAST,{method:'POST',headers:{'Content-Type':'text/plain;charset=UTF-8'},body:JSON.stringify({pin:PIN,action:'waitlist_list'}),signal:ctl.signal,cache:'no-store'}),j=await r.json();if(!r.ok||!j.ok)throw Error(j.error||'Ошибка ловца');return j.rows||[]}finally{clearTimeout(timer)}}
 async function openOverview(initial='active'){modal('Ловец окошек','Клиенты, которые ждут свободное время','<div id="wlStableBody" class="sub" style="padding:18px">Загружаю список…</div>','<button class="btn" id="wlStableClose">Закрыть</button>');document.getElementById('wlStableClose').onclick=closeModal;const host=document.getElementById('wlStableBody'),snap=window.SLOTELLY_SNAPSHOT?.waitlist;if(Array.isArray(snap)&&snap.length)renderRows(host,snap,initial);try{const rows=await api();renderRows(host,rows,initial);if(window.SLOTELLY_SNAPSHOT){window.SLOTELLY_SNAPSHOT.waitlist=rows;window.SLOTELLY_SNAPSHOT.waitlist_count=rows.filter(r=>['active','new','offered'].includes(r.status)).length}}catch(e){if(!(Array.isArray(snap)&&snap.length))host.innerHTML='<div class="card">Список временно недоступен. Попробуйте ещё раз.</div><button class="btn primary" id="wlStableRetry" style="width:100%;margin-top:8px">Повторить</button>';document.getElementById('wlStableRetry')?.addEventListener('click',()=>openOverview(initial))}}
 window.openWaitlistOverview=openOverview;const b=document.getElementById('homeWaitlistBtn');if(b)b.onclick=()=>openOverview('active');
})();