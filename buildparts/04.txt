n showAppt(a){let total=a.services.reduce((n,s)=>n+Number(s.price||0),0),p=a.payments?.[0];modal(a.client_name||'Запись',ld(a.starts_at)+' · '+hm(a.starts_at),'<button class="card" id="clientLink"><b>'+a.client_name+'</b></button><div class="card">'+a.services.map(s=>s.name+' — <b>'+Number(s.price).toLocaleString('ru-RU')+' ₽</b>').join('<br>')+'<hr>Итого <b>'+total.toLocaleString('ru-RU')+' ₽</b></div><div class="card">Комментарий: '+(a.master_comment||'—')+'</div><div class="card">Оплата: '+(p?Number(p.total).toLocaleString('ru-RU')+' ₽':'не проведена')+'</div>','<button class="btn primary" id="edit">Редактировать</button><button class="btn" id="pay">Оплатить</button><button class="btn" id="unpaid">Без оплаты</button><button class="btn" id="cancelA">Отменить</button>');clientLink.onclick=()=>clientCard(a.client_id);edit.onclick=()=>form(a,ld(a.starts_at),hm(a.starts_at));pay.onclick=()=>payment(a,total);unpaid.onclick=async()=>{await rpc('master_app_status',{p_pin:PIN,p_id:a.id,p_status:'completed_unpaid'});closeModal();renderCal()};cancelA.onclick=async()=>{if(confirm('Отменить запись?')){await rpc('master_app_status',{p_pin:PIN,p_id:a.id,p_status:'cancelled'});closeModal();renderCal()}}}
function form(a=null,ds=focus,time='16:00'){let cid=a?.client_id||'',rows=a?.services?.length?a.services.map(s=>({service_id:s.service_id,name:s.name,standard_price:s.standard_price,price:s.price,duration:s.duration})):[];if(!rows.length&&services[0])rows=[{service_id:services[0].id,name:services[0].name,standard_price:services[0].base_price,price:services[0].base_price,duration:services[0].duration_minutes}];
modal(a?'Редактирование':'Новая запись','', '<div class="field"><label>Клиент</label><input id="q" placeholder="Имя или телефон" value="'+(a?.client_name||'')+'"></div><div id="res" class="list"></div><div class="twocol"><div class="field"><label>Дата</label><input id="date" type="date" value="'+(a?ld(a.starts_at):ds)+'"></div><div class="field"><label>Время</label><input id="time" type="time" step="900" value="'+(a?hm(a.starts_at):time)+'"></div></div><div id="svcRows"></div><button class="btn" id="addSvc" style="width:100%">+ Добавить услугу</button><div class="field"><label>Комментарий</label><textarea id="com">'+(a?.master_comment||'')+'</textarea></div>','<button class="btn primary" id="save">Сохранить</button><button class="btn" id="newC">+ Новый клиент</button>');
function rr(){svcRows.innerHTML='';rows.forEach((r,i)=>{let x=document.createElement('div');x.className='card';x.innerHTML='<div class="field"><select class="ss">'+services.filter(s=>s.is_active).map(s=>'<option value="'+s.id+'" '+(s.id===r.service_id?'selected':'')+'>'+s.name+'</option>').join('')+'</select></div><div class="twocol"><div class="field"><label>Цена</label><input class="sp" type="number" value="'+r.price+'"></div><div class="field"><label>Длительность, мин</label><input class="sd" type="number" value="'+r.duration+'"></div></div>';let ss=x.querySelector('.ss'),sp=x.querySelector('.sp'),sd=x.querySelector('.sd');ss.onchange=()=>{let s=services.find(z=>z.id===ss.value);rows[i]={service_id:s.id,name:s.name,standard_price:s.base_price,price:s.base_price,duration:s.duration_minutes};rr()};sp.oninput=()=>rows[i].price=Number(sp.value||0);sd.oninput=()=>rows[i].duration=Number(sd.value||30);svcRows.appendChild(x)})}rr();
q.oninput=async()=>{if(q.value.trim().length<2){res.innerHTML='';return}let z=await rpc('master_app_clients',{p_pin:PIN,p_q:q.value});res.innerHTML=z.map(c=>'<button class="card pick" data-id="'+c.id+'"><b>'+c.name+'</b><div class="sub">'+c.phone+'</div></button>').join('');$$('.pick').forEach(b=>b.onclick=()=>{cid=b.dataset.id;q.value=b.querySelector('b').textContent;res.innerHTML=''})};addSvc.onclick=()=>{let s=services.find(z=>z.is_active);rows.push({service_id:s.id,name:s.name,standard_price:s.base_price,price:s.base_price,duration:s.duration_minutes});rr()};newC.onclick=()=>newClient(c=>{cid=c.id;q.value=c.name});save.onclick=async()=>{if(!cid)return alert('Выберите клиента');await rpc('master_app_save',{p_pin:PIN,p_id:a?.id||null,p_client:cid,p_start:new Date(date.value+'T'+time.value+':00+03:00').toISOString(),p_services:rows,p_comment:com.value,p_status:a?.status||'new'});closeModal();renderCal()}}
function newClient(done){modal('Новый клиент','', '<div class="field"><input id="nn" placeholder="Имя"></div><div class="field"><input id="np" placeholder="Телефон" type="tel"></div><div class="field"><select id="nm"><option>Telegram</option><option>WhatsApp</option><option>VK</option><option>MAX</option></select></div>','<button class="btn primary" id="ns">Сохранить</button>');ns.onclick=async()=>{let z=await edge('createClient',{name:nn.value,phone:np.value,messenger:nm.value});done?.({id:z.client.id,name:z.client.display_name||z.client.name});closeModal()}}
function payment(a,total){modal('Оплата',a.client_name||'', '<div class="field"><label>Наличные</label><input id="cash" type="number" value="0"></div><div class="field"><label>Карта</label><input id="card" type="number" value="'+total+'"></div><div class="field"><label>Другое</label><input id="other" type="number" value="0"></div>','<button class="btn primary" id="ps">Провести</button>');ps.onclick=async()=>{let c=Number(cash.value||0),d=Number(card.value||0),o=Number(other.value||0);if(c+d+o!==total)return alert('Разбивка должна быть '+total+' ₽');await rpc('master_app_payment',{p_pin:PIN,p_id:a.id,p_cash:c,p_card:d,p_other:o});closeModal();renderCal()}}
async function loadClients(){clientList.innerHTML='<div class="sub">Начните вводить имя или телефон</div>'}clientSearch.oninput=async()=>{if(clientSearch.value.trim().length<2){clientList.innerHTML='<div class="sub">Начните вводить имя или телефон</div>';return}let z=await rpc('master_app_clients',{p_pin:PIN,p_q:clientSearch.value});clientList.innerHTML=z.map(c=>'<button class="card cli" data-id="'+c.id+'"><b>'+c.name+'</b><div class="sub">'+c.phone+'</div></button>').join('');$$('.cli').forEac