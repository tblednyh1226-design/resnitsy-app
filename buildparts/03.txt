Start=monday(focus);screen('calendar')}
function modal(t,s,b,f='',clickable=false){mt.textContent=t;ms.textContent=s||'';mb.innerHTML=b;mf.innerHTML=f;mt.disabled=!clickable;ov.classList.add('on')}function closeModal(){ov.classList.remove('on')}document.getElementById('close').addEventListener('click',closeModal);document.getElementById('ov').addEventListener('click',e=>{if(e.target===document.getElementById('ov'))closeModal()});
async function init(){let b=await rpc('master_app_bootstrap',{p_pin:PIN});services=b.services||[];settings=b.settings||{};finFrom.value=add(todayStr(),-30);finTo.value=todayStr();renderSettings()}
function topPx(min,height){return 36+(min-540)/840*(height-36)}
async function renderCal(){
const weekTabEl=document.getElementById('weekTab'),dayTabEl=document.getElementById('dayTab'),weekEl=document.getElementById('week'),dayEl=document.getElementById('dayview');
weekTabEl.classList.toggle('on',view==='week');dayTabEl.classList.toggle('on',view==='day');
weekEl.style.display=view==='week'?'grid':'none';dayEl.classList.toggle('on',view==='day');
document.getElementById('calSub').textContent=view==='week'?'Неделя целиком':'День';
if(view==='week')await renderWeek();else await renderDay();
}
async function renderWeek(){let dates=[0,1,2,3,4,5,6].map(i=>add(weekStart,i)),from=D(weekStart).toISOString(),to=D(add(weekStart,7)).toISOString();lastCal=await rpc('master_app_calendar',{p_pin:PIN,p_from:from,p_to:to});document.getElementById('period').textContent=D(weekStart).toLocaleDateString('ru-RU',{day:'numeric',month:'short'})+' — '+D(add(weekStart,6)).toLocaleDateString('ru-RU',{day:'numeric',month:'short'});document.getElementById('week').innerHTML='';
for(const ds of dates){let col=document.createElement('div');col.className='day';col.innerHTML='<div class="dh"><strong>'+D(ds).toLocaleDateString('ru-RU',{weekday:'short'}).replace('.','').toUpperCase()+'</strong>'+D(ds).getDate()+'<button class="plus">+</button></div>';col.querySelector('.dh').onclick=e=>{if(!e.target.classList.contains('plus')){focus=ds;view='day';renderCal()}};col.querySelector('.plus').onclick=e=>{e.stopPropagation();extraTime(ds)};
let aps=lastCal.appointments.filter(a=>ld(a.starts_at)===ds&&a.status!=='cancelled');for(const a of aps){let st=hm(a.starts_at),m=+st.slice(0,2)*60+ +st.slice(3),dur=(new Date(a.ends_at)-new Date(a.starts_at))/60000,b=document.createElement('button');b.className='appt '+a.status;b.style.top=topPx(m,document.getElementById('week').clientHeight)+'px';b.style.height=Math.max(22,dur/840*(document.getElementById('week').clientHeight-36))+'px';b.innerHTML='<strong>'+st+' '+(a.client_name||'Клиент')+'</strong><small>'+a.services.map(s=>s.name).join(' + ')+'</small>';b.onclick=()=>showAppt(a);col.appendChild(b)}
for(const t of ['10:00','13:00','16:00','19:00']){let st=new Date(ds+'T'+t+':00+03:00'),busy=aps.some(a=>st>=new Date(a.starts_at)&&st<new Date(a.ends_at)),o=lastCal.overrides.find(x=>new Date(x.slot_start).getTime()===st.getTime()),on=o?o.is_available:true;if(!busy){let m=+t.slice(0,2)*60+ +t.slice(3),b=document.createElement('button');b.className='free '+(on?'':'off');b.style.top=topPx(m,document.getElementById('week').clientHeight)+'px';b.textContent=t;b.onclick=async()=>{await rpc('master_app_set_availability',{p_pin:PIN,p_slot:st.toISOString(),p_value:!on});renderWeek()};col.appendChild(b)}}document.getElementById('week').appendChild(col)}}
async function renderDay(){let z=await rpc('master_app_calendar',{p_pin:PIN,p_from:D(focus).toISOString(),p_to:D(add(focus,1)).toISOString()});document.getElementById('period').textContent=D(focus).toLocaleDateString('ru-RU',{weekday:'long',day:'numeric',month:'long'});document.getElementById('dayview').innerHTML='';for(let h=9;h<23;h++){let row=document.createElement('div');row.className='hourrow';row.innerHTML='<div class="hour">'+String(h).padStart(2,'0')+':00</div><div class="lane"><div class="half"></div></div>';let lane=row.querySelector('.lane');for(let mm of [0,30]){let t=String(h).padStart(2,'0')+':'+String(mm).padStart(2,'0'),st=new Date(focus+'T'+t+':00+03:00'),o=z.overrides.find(x=>new Date(x.slot_start).getTime()===st.getTime()),def=['10:00','13:00','16:00','19:00'].includes(t),on=o?o.is_available:def,b=document.createElement('button');b.className='dfree '+(on?'on':'');b.style.top=mm===0?'2%':'52%';b.textContent=t+(on?' доступно':' закрыто');b.onclick=async e=>{e.stopPropagation();await rpc('master_app_set_availability',{p_pin:PIN,p_slot:st.toISOString(),p_value:!on});renderDay()};lane.appendChild(b)}lane.onclick=e=>{if(e.target===lane||e.target.classList.contains('half')){let rect=lane.getBoundingClientRect(),mm=e.clientY-rect.top<rect.height/2?'00':'30';form(null,focus,String(h).padStart(2,'0')+':'+mm)}};document.getElementById('dayview').appendChild(row)}
for(const a of z.appointments.filter(x=>x.status!=='cancelled')){let st=hm(a.starts_at),m=+st.slice(0,2)*60+ +st.slice(3),dur=(new Date(a.ends_at)-new Date(a.starts_at))/60000,b=document.createElement('button');b.className='dappt '+a.status;b.style.top=((m-540)/840*100)+'%';b.style.height=Math.max(3,dur/840*100)+'%';b.innerHTML='<strong>'+st+' · '+(a.client_name||'Клиент')+'</strong><small>'+a.services.map(s=>s.name).join(' + ')+'</small>';b.onclick=()=>showAppt(a);document.getElementById('dayview').appendChild(b)}}
document.getElementById('weekTab').addEventListener('click',()=>{view='week';weekStart=monday(focus);renderCal()});
document.getElementById('dayTab').addEventListener('click',()=>{view='day';renderCal()});
document.getElementById('prev').addEventListener('click',()=>{if(view==='week'){weekStart=add(weekStart,-7);focus=weekStart}else{focus=add(focus,-1)}renderCal()});
document.getElementById('next').addEventListener('click',()=>{if(view==='week'){weekStart=add(weekStart,7);focus=weekStart}else{focus=add(focus,1)}renderCal()});
document.getElementById('todayBtn').addEventListener('click',()=>{focus=todayStr();weekStart=monday(focus);renderCal()});
functio