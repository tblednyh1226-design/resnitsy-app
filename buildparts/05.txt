h(b=>b.onclick=()=>clientCard(b.dataset.id))};document.getElementById('addClientBtn').onclick=()=>newClient();
async function clientCard(id){let d=await edge('history',{clientId:id}),c=d.client,done=d.history.filter(x=>x.status==='completed_paid'||x.status==='completed_unpaid'),last=done.length?new Date(done[0].starts_at).toLocaleDateString('ru-RU',{timeZone:'Europe/Moscow'}):'—';modal(c.display_name,'Карточка клиента','<div class="card">Телефон: <b>'+c.phone+'</b><br>Последний визит: <b>'+last+'</b></div><div class="list">'+d.history.map(h=>'<button class="card hist" data-id="'+h.id+'"><b>'+new Date(h.starts_at).toLocaleString('ru-RU',{timeZone:'Europe/Moscow'})+'</b><div class="sub">'+h.appointment_services.map(s=>s.service_name_snapshot).join(' + ')+' · '+h.status+'</div></button>').join('')+'</div>');$$('.hist').forEach(b=>b.onclick=async()=>{let a=(await edge('appointment',{id:b.dataset.id})).appointment;showAppt({id:a.id,client_id:a.client_id,client_name:a.clients?.display_name,starts_at:a.starts_at,ends_at:a.ends_at,status:a.status,master_comment:a.master_comment,services:a.appointment_services.map(s=>({service_id:s.service_id,name:s.service_name_snapshot,standard_price:s.standard_price_snapshot,price:s.actual_price,duration:s.duration_minutes})),payments:(a.payments||[]).map(p=>({total:p.total_amount}))})})}
async function runFinance(){let z=await edge('report',{from:new Date(finFrom.value+'T00:00:00+03:00').toISOString(),to:new Date(add(finTo.value,1)+'T00:00:00+03:00').toISOString()}),rows=z.rows.filter(x=>x.status!=='cancelled'),paid=rows.filter(x=>x.status==='completed_paid'),money=paid.reduce((n,x)=>n+(x.payments?.[0]?Number(x.payments[0].total_amount):0),0),clients=new Set(rows.map(x=>x.client_id));finBox.innerHTML='<div class="metricgrid"><div class="card"><div class="metric">'+money.toLocaleString('ru-RU')+' ₽</div><div class="sub">выручка</div></div><div class="card"><div class="metric">'+rows.length+'</div><div class="sub">записей</div></div><div class="card"><div class="metric">'+clients.size+'</div><div class="sub">клиентов</div></div><div class="card"><div class="metric">'+(paid.length?Math.round(money/paid.length).toLocaleString('ru-RU'):0)+' ₽</div><div class="sub">средний чек</div></div></div>'}document.getElementById('finRun').onclick=runFinance;
async function loadReports(){let w=await edge('waitlist'),r=await edge('report',{from:new Date(add(todayStr(),-30)+'T00:00:00+03:00').toISOString(),to:new Date(add(todayStr(),1)+'T00:00:00+03:00').toISOString()});reportBox.innerHTML='<button class="card"><b>Ловец окошек</b><div class="sub">Активных запросов: '+(w.rows?.length||0)+'</div></button><div class="card"><b>Записи за 30 дней</b><div class="sub">'+(r.rows?.length||0)+' записей</div></div>'}
async function loadServicesAdmin(){let z=await serviceApi('list');services=z.services||[];categories=z.categories||[];renderServices()}
function catName(id){return categories.find(c=>c.id===id)?.name||id||'Без категории'}
function renderServices(){
  categoryList.innerHTML=categories.sort((a,b)=>(a.sort_order||0)-(b.sort_order||0)).map(c=>'<div class="card"><div style="display:flex;justify-content:space-between;gap:8px;align-items:center"><div><b>'+c.name+'</b><div class="sub">'+(c.is_active===false?'неактивна':'активна')+'</div></div><div style="display:flex;gap:5px"><button class="btn catEdit" data-id="'+c.id+'">Изменить</button><button class="btn catToggle" data-id="'+c.id+'">'+(c.is_active===false?'Включить':'Выключить')+'</button></div></div></div>').join('');
  serviceList.innerHTML=services.sort((a,b)=>(a.sort_order||0)-(b.sort_order||0)).map(s=>'<div class="card"><div style="display:flex;justify-content:space-between;gap:8px"><div><b>'+s.name+'</b><div class="sub">'+catName(s.category)+' · '+Number(s.base_price).toLocaleString('ru-RU')+' ₽ · '+s.duration_minutes+' мин</div><div class="sub">'+(s.is_active?'активна':'неактивна')+' · '+(s.is_online_bookable?'видна в онлайн-записи':'только мастер')+'</div></div><button class="btn svcEdit" data-id="'+s.id+'">Изменить</button></div></div>').join('');
  $$('.catEdit').forEach(b=>b.onclick=()=>categoryForm(categories.find(c=>c.id===b.dataset.id)));
  $$('.catToggle').forEach(b=>b.onclick=async()=>{let c=categories.find(x=>x.id===b.dataset.id);await serviceApi('toggleCategory',{id:c.id,is_active:c.is_active===false});await loadServicesAdmin()});
  $$('.svcEdit').forEach(b=>b.onclick=()=>serviceForm(services.find(s=>s.id===b.dataset.id)));
}
function categoryForm(c=null){modal(c?'Категория':'Новая категория','', '<div class="field"><label>Название</label><input id="catN" value="'+(c?.name||'')+'"></div><div class="field"><label><input id="catA" type="checkbox" '+(c?.is_active===false?'':'checked')+'> Активна</label></div>','<button class="btn primary" id="catSave">Сохранить</button>'+(c?'<button class="btn" id="catDelete">Удалить</button>':''));catSave.onclick=async()=>{await serviceApi('saveCategory',{id:c?.id||null,name:catN.value,is_active:catA.checked,sort_order:c?.sort_order||null});closeModal();await loadServicesAdmin()};if(c)catDelete.onclick=async()=>{if(confirm('Удалить категорию?')){try{await serviceApi('deleteCategory',{id:c.id});closeModal();await loadServicesAdmin()}catch(e){alert(e.message)}}}}
function serviceForm(s=null){let activeCats=categories.filter(c=>c.is_active!==false||c.id===s?.category);modal(s?'Редактирование услуги':'Новая услуга','', '<div class="field"><label>Название</label><input id="svcN" value="'+(s?.name||'')+'"></div><div class="field"><label>Категория</label><select id="svcC">'+activeCats.map(c=>'<option value="'+c.id+'" '+(c.id===s?.category?'selected':'')+'>'+c.name+'</option>').join('')+'</select></div><div class="twocol"><div class="field"><label>Цена, ₽</label><input id="svcP" type="number" value="'+(s?.base_price||0)+'"></div><div class="field"><label>Длительность, мин</label><input id="svcD" type="number" value="'+(s?.duration_minutes||60)+'"></div></div><div class="twocol"><div class=